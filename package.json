{
  "name": "story-builder",
  "version": "1.0.0",
  "private": true,
  "description": "Guided content authoring tool for the match-3 game (chapters + shards). No JSON or coding required.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "verify": "node scripts/verify_roundtrip.js",
    "build:engine": "node build-webapp.js",
    "build:seed": "node build-seed.js",
    "build:www": "node build-webapp.js && node build-seed.js && node build-www.js",
    "android:sync": "npm run build:www && npx cap sync android",
    "android:apk": "bash build-android.sh"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/share": "^8.0.1",
    "archiver": "^7.0.1",
    "express": "^4.19.2",
    "jszip": "^3.10.1",
    "multer": "^1.4.5-lts.1"
  }
}
